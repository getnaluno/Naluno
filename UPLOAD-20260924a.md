# Naluno 2026.09.24a

Unzip into the GitHub repo root so these files land in the folders that already exist (`app/`, `js/`, `css/`, `admin/`, `workers/economy/`). Do not upload a new folder named `public`.

Deploy separately, after the Pages files:
- Economy worker `workers/economy/handler.mjs` (2.6.10-play). Views, reactions, and the old share page depend on it.
- `firestore.rules`. Until this is published, Seen by and reactions still cannot be saved.

Leave alone (not in this zip): `js/wireline.js`, `js/calls.js`, `js/lifeline.js`, `js/lifeline-wire.js`, `js/wireline-translate.js`.
