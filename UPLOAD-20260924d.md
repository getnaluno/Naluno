# Naluno 2026.09.24d

Unzip into the GitHub repo root so these files land in the folders that already exist (`app/`, `js/`, `css/`, `admin/`). Do not upload a new folder named `public`.

Publish `firestore.rules` with this pack. Without them, a new admin cannot be saved, and Seen-by rules from earlier packs stay as they were.

The economy worker is unchanged (`2.6.10-play`). Do not redeploy it.

What this pack changes:

- Support creator on a Broadcast is one row: the name, and Off or On. The explanation is behind that row.
- The rest of the room stays quieter. View lines, the description, and Strand open when you tap them.
- Advertise opens on top of the video. After save it says: "Your ad awaits a review. Once confirmed it will go live." The app does not mention the console.
- Phone Back from a Broadcast brings the bottom tabs back. Other screens were already doing this.
- Watch on a Signal opens that Broadcast on the first tap, from the copy already on the phone when it has one.
- The existing operator is the superadmin. In the console, Admins creates other logins and chooses which sections they can open. They cannot revoke, overwrite, or delete the superadmin.
- Books compiles a trial balance and a journal. Download CSV, Excel, PDF, or JSON for an accountant. These are booked amounts, intents, and estimates. Cash has not moved.
