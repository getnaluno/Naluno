# Naluno 2026.09.25a

Unzip into the GitHub repo root. Do not upload a folder named `public`.

Publish `firestore.rules` with this pack. Without them, taste and events cannot be saved, and the desk cannot compile features.

The economy worker is unchanged. Do not redeploy it.

For You now ranks the Broadcasts already loaded. It does not rank by follower count, and it does not try to keep someone scrolling.

- A Broadcast is one object: the film, its chapters, its Strand, and the conversation on it.
- Order is four steps: possible Broadcasts, then what this person should not see, then a score for this person, then a feed that keeps room for unfamiliar work.
- Weights live on named models (`ranker_v1`, `ranker_v2`). The desk can try the second model with a percent of people. That is not a permanent formula.
- On a Broadcast, More → Why this. More like this, less of this, not interested, and hide this creator are in that sheet.
- The console has a Discovery section. Save the live model there. Compile reads recent events and writes one feature row per Broadcast. Phones do not scan the catalogue.
