# 2026.09.25b

Unzip into the repo root. Do not upload a folder named public.

Publish firestore.rules with this pack.

Then publish two workers, or the new steps stay dark:

1. Economy worker, from workers/economy (the file that already serves naluno-economy).
   Secrets, only in the worker, never in the app:

   npx wrangler secret put STRIPE_SECRET_KEY
   npx wrangler secret put STRIPE_WEBHOOK_SECRET
   npx wrangler secret put CF_CALLS_APP_ID
   npx wrangler secret put CF_CALLS_APP_SECRET

   Stripe webhook URL: https://naluno-economy.naluno.workers.dev/v1/pay/webhook
   Event: checkout.session.completed

   Until those two Stripe secrets and the service account are both set, the payment step says it is not available and nothing is marked paid.

   Until the Cloudflare Realtime app id and secret are set, a live Broadcast stays a direct room of 12. It does not pretend to be larger.

2. The upload worker (signal-worker-index.js), on both naluno-signal-upload and naluno-broadcast-upload if they are separate. That is what removes a person's files when they close a Callsign.

Creator Support stays off until you turn it on. Turning it on does not mark anything paid by itself.
