rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    /* Naluno media lives on R2, not Firebase Storage. Deny everything
       so a forgotten bucket cannot become an open dump. */
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
