name = "naluno-turn-credentials"
main = "index.js"
compatibility_date = "2026-01-01"

# TURN_KEY_API_TOKEN is NOT here on purpose — it's a genuine long-term secret and must
# only ever be set via:
#   npx wrangler secret put TURN_KEY_API_TOKEN
# Putting it in this file would mean it sits in plain text on disk, defeating the
# point of it being a secret at all.

[vars]
TURN_KEY_ID = "2248e28dd61fabf8235b3a47d1f6314a"
FIREBASE_WEB_API_KEY = "AIzaSyD0j1W7-gFJqbMd6rz4kMhQd5AiB8B2ox0"
